## Design an Algorithm that:

1. Takes as input an array of numbers.
2. Find the smallest number from the array.
3. Removes it from input array & adds it to new `sortedArray`
4. Add it to a `sortedArr` and repeat 
5. Stop when input array is empty.
6. Output `sortedArr`.
